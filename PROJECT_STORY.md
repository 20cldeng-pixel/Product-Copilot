# 从需求变更到可验证交付

## 产品问题

自然语言开发能快速生成代码，但用户仍然需要回答三个问题：Agent 改的是不是我的需求？旧数据和旧规则是否保留？“完成”有没有可复核的证据？

Product Copilot 用一个活动报名案例研究这些问题。第一版只能报名一场；随后用户要求每场限报一次，并保留容量、关闭状态和旧报名。这个变更同时涉及需求语义、数据迁移、界面资格和回归测试，适合观察整个产品链路。

## 设计取舍

**把确认放在范围与原型上。** 模型可整理草案和提案，批准由用户操作触发。批准不是永久开关：对应内容变化后需要重新确认。

**把完成与通过分开。** Builder 正常结束只表示开发回合结束；Runner 收集实际测试结果，再关联到验收条件。未判定和失效状态让用户看到证据缺口，人工核验保留独立记录。

**让变更保留上下文。** 提案记录前后需求、影响依据及不应改变的规则。当前影响记录不是完整静态依赖分析，也不承诺精确定位每一行代码。

**先做小闭环。** 采用本地桌面状态与现有 Agent 引擎，没有为了角色数量引入额外工作流框架。复盘先提供可复算统计，再由用户显式触发模型解读。

**把长开发回合拆成可恢复批次。** 宿主按已批准的 P0 顺序每批最多交给 Builder 两项，并把需求 ID 固化在运行记录中。正常结束后推进下一批，失败或取消后重试原批次；全部批次跑过后才进入集成补缺。批次完成只是执行进度，仍需独立验收。

## 已有证据

Gather 完成 V1 到 V2 的真实应用链路，当前示例有 36 项测试，产品计划关联的 26 项 P0 条件通过。独立探针覆盖 7 项业务/迁移条件；页面另行验证重复报名、跨活动、满额、关闭及刷新后的保存。

验收器经过五样本公开校准：正确 V2 通过，未改旧规则、移除去重、误限一人及丢弃旧数据四类缺陷被识别。这不是未见测试集。

原版 EasyMint 与改进版在首轮单次预实验中都完成了同一变更，独立页面检查均为 11/11。随后用同一模型、600 秒墙钟和 80 次工具上限各重复 3 次，两组独立 VC 探针仍均为 3/3；B 的平均时长、工具调用和未核账 cost 估算也没有更低。作者自建的独立未见 DeskFlow 案例中，原始需求核心两组仍均 3/3；扩展合同是 A 0/3、B 3/3，但差异只来自合同额外要求的 schema 升版与新版修复，而数据形态并未变化。该结果证明合同能提高对明确治理条款的遵从，不能证明同一用户任务成功率提升。

完整工作流实验又从相同空项目执行 ShiftLoop 的 Idea、澄清、V1、必需改变数据形态的 V2、迁移验收与复盘。A/B 各 3 次，在每轮 300 秒上限下均为 0/3；A 都在首轮直接实现时超时，B 分别到第 2 或第 3 轮，但两轮仍绕过原型确认提前实现。复查发现该 B 组只是提示词协议，并未经过正式产品已有的主进程阶段闸门。实验后把正式 Builder 改成每批最多 2 项 P0，并在真实桌面入口的新案例中完成 2/1 两批及一次集成补缺。独立验收又发现自动测试没有覆盖“页面无法产生已完成状态”，返工 1 次后才取得 10/10 测试与 9/9 P0 关联通过。这次返工还暴露了产品只能停止开发、不能从失败验收返回 Builder；随后补成“失败条件 → 相关需求修复 → 重新集成 → 再验收”的闭环。该 smoke 验证了运行路径和验收发现缺陷的价值；它没有重复样本或对照组，仍不能回答端到端完成率是否提升。

随后用正式桌面入口预注册 EVAL-04。A1 在 45 分钟内未完成；B1 虽完成三个需求批次和一次集成，但修复阶段读取了 A1 文件，被排除出主分析；B2 因操作者输入偏离冻结脚本而作废。其余三轮按决定不再执行，因此实验没有有效的桌面 A/B 配对。这个结果没有被合并进先前数据，也没有被包装成效果提升；它进一步暴露了运行隔离、测试框架适配和长链路收敛问题。

## 真实发现的体验问题

原型修改后，后台已经拒绝开发，页面仍显示“开发已获授权”。这会让用户误以为按钮失灵。修复后，页面从当前内容派生批准有效性，标题、开发入口和复盘确认数保持一致；历史批准仍然保留。

## 如何演示

1. 打开 Gather 产品计划，展示已确认规则及原型。
2. 查看 CHG-001 的前后差异和保留项。
3. 展示需求/原型改变时旧批准或验收失效的状态。
4. 查看真实开发记录、验收用例关联及未判定/通过的区别。
5. 用页面演示同场重复拒绝、跨场成功和旧数据保留。
6. 展示本地试用复盘，并说明模拟身份和数据来源边界。

## 待验证的假设

- 对复杂或含糊变更，结构化提案是否减少遗漏？
- 证据关联是否降低错误宣告通过的比例？
- 多出的确认和接入成本是否被返工减少抵消？
- 中断或环境错误后，用户能否理解状态并恢复执行？

目前不填写成功率提升或节省成本百分比；公开固定案例、未见案例原始需求核心和完整工作流的组间成功率差异均为 0 个百分点。未见案例的扩展合同差异受到额外 schema 条款混杂；完整工作流两组都未完成，真实人工耗时与返工次数仍未知。

# 2026-09-20

## 1. 首版正式开发启动（未提交）

- 用户要求读取产品计划 revision 7、批准原型和技术可行性预审后正式开发。
- 已确认产品计划阶段为 development_authorized，范围为全部 P0 与原型展示的 P1，不实现 P2。
- 项目中未发现独立的上一轮技术预审文件；以批准计划中的约束和本轮明确的规范化状态、异常恢复与回滚要求作为实现基线。
- 技术方案采用 React + TypeScript + Vite，领域状态变更集中为纯函数，Repository 管理 schemaVersion、完整校验、损坏恢复和写失败回滚。

## 2. 关键交互测试与交付配置完成（工作区无 Git 元数据）

- 使用 Testing Library 覆盖创建活动成功与字段校验、管理员切换状态、空与非空名单、已报名身份提示和成功报名。
- 为创建活动、切换状态和成员报名分别验证 Repository 写入失败后的界面回滚，断言不显示成功反馈且业务状态不变。
- 新增 `.easymint/run.json`，提供本地开发、运行测试、生产构建和预览构建四个常用入口。
- 完整测试共 26 项通过，生产构建通过；首版实施方案已在实现完成后回收。

## 3. 暂停后恢复：独立浏览器验证与收尾

- 在内置浏览器访问生产预览 `http://127.0.0.1:4173/`，验证容量 0 被拒绝、容量 3 创建 A4 成功；未选身份、满员 A2、关闭 A3 均拒绝报名。
- U1 成功报名 A1 后，A1 为 1/2，跨活动报名被拒绝。刷新后 A4 与 U1 报名保留；关闭 A1 后名单仍包含 U1，关闭状态在再次刷新后保留。
- 390px 成员视图为一列、800px 为两列，实测页面宽度分别为 390/800，无横向溢出；已检查截图。
- 修复大容量活动按每个名额创建 DOM 元素的问题：20 以内保留座位条，超过 20 使用比例进度条，真实容量和人数不变。增加百万容量回归用例。
- 运行入口指定 5174，避免与 EasyMint 的 5173 冲突；生产预览入口先构建再运行，使用 4173。
- 使用 npm 锁文件作为安装基线；清理本次误用 pnpm 生成的额外锁文件，并通过 npm ci 恢复一致依赖。
- 最终测试为 3 文件、27 项通过，生产构建通过。
- 写入失败三条路径与损坏快照恢复由自动化测试验证，本轮未在浏览器中注入存储异常；证据仅覆盖单浏览器固定案例。
- EasyMint 任务状态 done 与独立执行证据分别记录；尚未接入产品计划 Run/Evidence 自动验收，不能推断跨案例成功率。

## 4. CHG-001 逐活动唯一报名

- 将 `registrationsByUserId` 从 `userId → eventId` 升级为 `userId → eventId[]`，报名命令仅拒绝同一身份对当前活动的重复报名；容量、关闭状态和失败无副作用语义保持不变。
- 成员视图按活动显示已报名与可报名状态，已报名其他活动不再禁用当前活动；管理概览按全部报名记录计数。
- localStorage 快照升级至 schemaVersion 2；有效 V1 快照逐条迁移并回写，回写失败仍返回完整迁移数据，不进入损坏恢复。
- 自动化测试为 3 个文件、30 项通过，生产构建通过。本轮未启动服务、未执行浏览器交互，因此不声明 CHG-001 业务验收通过。

## 5. T6 本地体验事件

- 增加与业务快照分离的本地事件记录：选择模拟身份时记录各活动浏览，提交时记录提交与最终成功/拒绝；事件包含 ID、会话、模拟身份、活动、产物版本、时间及来源。普通页面标为 `local_trial`；显式使用 `?dataSource=synthetic` 的演示页面标为 `synthetic`。
- 事件存储失败只显示“体验事件未完整保存”，不会阻断已成功的报名或改变业务状态；新增该失败路径的组件测试。
- 页面提供“导出本地体验事件”JSON。真实 4173 页面以 U1/U2 试用并导出 10 条事件（8 浏览、1 提交、1 成功），可由产品计划导入复算。该试用使用模拟身份，不代表真实用户增长。
- 本轮测试 3 文件、36 项通过，TypeScript 与 Vite 生产构建通过。业务规则及 P0 验收断言未修改；新产物仍须经正式 Runner 重跑才能更新当前验收状态。

# CHG-001：重复报名规则的作用域变更

提案版本：2。状态：**待页面核验与应用内确认**。现已核查 Gather V1 实际源码、通过领域/持久化基线并生成真实 V1 旧数据样本；尚未记录本提案的用户确认、正式 Run 或 V2 通过结果。

公开依据：[Starter BR-BASELINE v1](./01_Starter_行为基线.md)。目标合同：[REQ-REG-001 v2](./03_REQ-REG-001_v2_验收合同.md)。

## 1. 这次改什么

用户请求：“把规则改成每个用户每场活动只能报名一次。”

- **原规则 BR-03@v1**：一个用户在整个系统中最多成功报名一场活动。
- **目标规则 BR-03@v2**：同一用户不能重复报名同一活动；可以报名不同活动。不同用户报名同一活动互不构成重复，仍受活动容量限制。
- **Why**：可选。当前请求已足够定义变化，不要求用户额外填写动机。未提供的业务收益不写成事实。

这次不引入取消报名、截止时间、管理员特权、真实鉴权或并发保证。

## 2. 哪些行为必须保持

保持 BR-01、BR-02、BR-04、BR-05、BR-06、BR-07。既要保留活动浏览和开放状态，也要保留容量、计数、失败无副作用、刷新及既有数据。

BR-03@v1 被明确替代，不保留“跨活动也拒绝”的旧断言。其余回归测试不能因实现不通过而自动删除、跳过或放宽。

## 3. 影响预览

核查对象：Gather 提交 `4b1aa611c1b5a9888987a617da526f693513b09c`，工作树干净。下列关联来自真实源码阅读，不能保证穷尽所有影响。摘要为该文件 SHA-256 前 12 位，完整清单见[基线报告](../../evaluation/gather/v1-baseline-20260920/report.json)。

| 对象 | 文件与版本摘要 | 实际依据及变更边界 |
|---|---|---|
| 报名关系结构 | `app/domain/types.ts` · `10a6d8fe5629` | 已核验直接关联：registrationsByUserId 只能存 userId→单个活动，需支持一个用户关联多个活动 |
| 重复报名判断 | `app/domain/commands.ts` · `4623cd5a3034` | 已核验直接关联：registerForEvent 发现该用户任何已有报名便拒绝，需要改为目标活动内去重 |
| 人数、名单与用户关系 | `app/domain/selectors.ts` · `9e11f46885da` | 已核验直接关联：名单按单个活动值筛选；用户关系只返回一场，需同步适配且保留按活动计数 |
| 旧数据兼容 | `app/repository/localStorageRepository.ts` · `904cd3920eb4` | 已核验直接关联：仅接受 schemaVersion=1 和旧结构；不兼容时恢复初始数据，因此 V2 必须显式读取旧快照，不能把有效旧数据误当损坏 |
| 默认示例数据 | `app/domain/fixtures.ts` · `9b28ee6f1152` | 已核验：U2/U3 初始报名 A2 的数据采用旧结构；展示默认值必须随结构适配，独立测试继续使用公开 F0/F1/F2 |
| 按钮、状态与统计 | `app/App.tsx` · `995804702cf3` | 已核验直接关联：全局已有报名会禁用其他活动；成功报名统计按用户键计数，相关文案和关系判断需同步调整 |
| 领域测试 | `app/domain/domain.test.ts` · `f44614ede495` | 已核验：保留容量、关闭、失败无副作用等检查，只替换明确被废弃的全局唯一预期 |
| 持久化测试 | `app/repository/localStorageRepository.test.ts` · `29cca20d3001` | 已核验：已有版本/结构和损坏恢复测试；新增真实 V1 快照升级并再次加载 |
| 组件测试 | `app/App.test.tsx` · `76bda3c5339e` | 已核验：包含全局报名资格文案与旧状态结构，应跟随已确认新行为更新 |

原型 `prototype/index.html` 仍展示 V1 全局规则，确认新范围后需同步修改并重新批准；原型通过不替代正式应用验收。旧约束和问题答案必须一并更新，避免 PRD 同时要求“跨场成功”和“全局限一次”。

影响预览不减少本案例核心回归范围。没有发现某项影响不等于证明其不存在。

## 4. 未决问题与假设

- **阻塞性业务歧义：当前没有。** “每个用户每场活动一次”已经表达允许跨场活动；再次询问该点属于确认性提问，不算发现了新的关键歧义。
- 沿用 BR 对用户身份、开放状态、名额及保存行为的定义，不重复向用户询问。
- **执行准备阻塞：存在。** 领域/持久化基线和文件依据已补齐；当前 VC-05 展示部分及 VC-08 选用固定人工页面检查，检查完成前保持 not_run。需要加载新版应用并通过真实入口审阅/确认提案及原型。
- 不把“暂无业务问题”解释为“已经可以执行”。

## 5. 用户确认的内容

确认卡同时展示：

1. 同场不能重复、跨场可以报名、不同用户相互独立。
2. 保留全部未变更 BR，已有报名数据不能丢失。
3. 影响分析的确定项和不确定项。
4. VC-01～VC-08 的验证计划及需要人工检查的项目。

当前实现分为三步：确认变更范围 → 修改并重新批准原型 → 启动开发。确认范围后才记录实际批准、更新正式需求并使旧结果失效；启动开发时才生成 Run。尚未实现早期设计中的单按钮“确认并执行”，界面或项目说明不得宣称已实现。

业务规则或验收计划实质变化时重新确认；只更新运行日志不要求重复确认。用户拒绝或取消不构成授权。

## 6. 结果与异常交互

- Builder 完成：显示“修改完成，正在验证”，不立即显示通过。
- 证据符合当前版本：逐条展示通过，并提供日志/截图入口。
- 明确业务失败：展示对应标准、实际行为和有限修复选项。
- 环境错误或证据不足：显示“未能判定”，不把它包装成产品功能失败。
- 用户可按条目手动验证，或“接受当前版本并保留例外”；两者另记决定，不改变自动测试事实。
- 新变更确认后，旧结果归档；旧 Run 的迟到通知不能把新版本变绿。


## 7. 2026-09-20 变更前的实际证据

- 使用[可复跑探针](../../evaluation/gather/probe-v1.mjs)对实际 V1 执行 8 项数据/业务/持久化检查，全部通过。页面展示与浏览器刷新未由该探针验证，因此不能将它们记为整体基线已通过。
- [F1 原始快照](../../evaluation/gather/v1-baseline-20260920/f1-v1-storage.json)由公开 F0 经真实 V1 的 registerForEvent(U1,A1) 和 Repository.save 生成；不是由 V2 手写模拟迁移结果，也不含用户浏览器数据。
- 保持 VC-03 跨场成功断言，用 V1 执行得到 ALREADY_REGISTERED，按预期触发断言失败。这是旧规则反例校准通过，不代表 V2 已实现或已经验收。正确 V2、取消去重、清空旧数据三类校准仍待执行。
- VC-07 必须使用上述旧快照验证升级；V2 读取后先核对旧记录，再验证同场拒绝、跨场成功及再次加载。
- 原页面中用户已有的数据和批准未被脚本修改。探针结果未写入产品计划正式 Evidence。

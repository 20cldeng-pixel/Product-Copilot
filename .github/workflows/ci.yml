name: CI

# 零质量门禁：分支 push / pull_request 时跑 lint（eslint + 三段 tsc）与单元测试。
# 限定 branches（不含 tag）：发版时 tag 与 main 指向同一 commit，裸 on: push 会让
# 同一次提交重复跑两遍（tag 那次与 main 那次完全等价）。
# paths-ignore：纯文档提交（README/CHANGELOG/CLAUDE.md 等）无 lint 与单测可查，跳过；
# 提交里只要有一个非 .md 文件即照常触发。docs/ 与 .easymint/ 已 gitignore、不进仓库，无需列出。

on:
  push:
    branches: ['**']
    paths-ignore: ['**/*.md']
  pull_request:
    paths-ignore: ['**/*.md']

permissions:
  contents: read

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '22'

      # 项目 .npmrc 指向 npmmirror(国内本地安装加速),CI runner 在海外——环境变量覆盖回官方源
      - name: Install dependencies
        run: npm ci
        env:
          npm_config_registry: https://registry.npmjs.org/
          npm_config_electron_mirror: https://github.com/electron/electron/releases/download/

      - name: Lint & typecheck
        run: npm run lint

      # 沙盒相关用例需要 Linux 系统依赖（bwrap/socat/rg）；Ubuntu 24.04+ 还要放开 bwrap 建 userns。
      # 否则 quality-gate 必然红（与 sandbox-smoke.yml 同一套前置）。该 sysctl 在部分内核上不存在 → 容忍跳过。
      - name: 安装沙盒系统依赖
        run: sudo apt-get update && sudo apt-get install -y bubblewrap socat ripgrep
      - name: 放开 unprivileged userns（Ubuntu 24.04+ 默认限制 bwrap）
        run: sudo sysctl -w kernel.apparmor_restrict_unprivileged_userns=0 || echo "该内核无此开关，跳过"

      - name: Unit tests
        run: npx vitest run

# EVAL-01 配对预实验（2026-09-21）

状态：A 组已完成并冻结，独立浏览器评分 11/11 通过；B 组尚未启动。此为实验流程校准，不计入正式重复实验。

- A：原版 EasyMint 固定提交 `1cdc0c80c610539688aed0a54d45f23e991d8b18`，独立 worktree。
- B：改进版固定提交 `c4cd4d9`。
- 两份 Gather Starter 由同一 V1 提交归档生成，不包含 V2 Git 历史。32 个源文件逐字节一致，清单见 preparation.json。两组独立 npm ci，均 27/27 测试和生产构建通过。
- 两组应用使用同一依赖安装（上游与改进版 package.json/package-lock.json 无差异）；项目源码、node_modules 和会话独立。供应商全局配置共享，没有复制或导出凭据。
- 模型界面观察为 GPT-5.6 Sol，思考中，标准权限；当前增强 UI 选项按原有默认。实际工具与会话使用记录在结束后从对应日志提取。
- 共同首次输入：把规则改成每个用户每场活动只能报名一次。
- 澄清回答沿用 EVAL-01。A 的首次明确响应正确表达逐活动去重并保留其他规则；随后写出的方案明确包含合法 V1 快照迁移。目前没有人工纠错。

限制：20 分钟整体观察上限为预实验设置，原版工具调用上限尚无同等强制实现；改进版 Builder 内置 10 分钟/80 次工具限制。必须记录这一差异，不能称为严格等预算实验。B 的既有项目需求导入成本需另计，不能额外注入只有 B 可见的 V2 答案。SDK cost 字段仅作估算，不视为实际账单。

评分采用 EVAL-01 页面评分清单；本操作者知道组别，注明非盲评分。评分后不把错误反馈同一轮继续修复。未来正式实验重新冻结配置、创建新副本、保留所有运行，不能把这次预实验调参后的成功混入正式分母。

## A 组预实验结果

原版自报通过，独立页面评分亦通过（11/11），详见 A-freeze.json、A-session-summary.json、A-score.json。会话消息跨度 422.428 秒，70 次工具返回，0 次工具错误；只发出初始需求，无追加澄清或纠错。SDK 估算 cost 2.572533，币种及实际账单未核验，不能等同实际支出。未进行盲评，不能以单次成功推断总体成功率，也没有证据宣称改进版更优。原始日志与源码归档仅本地保存于忽略目录。

GitHub 作品仓库名称由用户确定为 **Product-Copilot**。

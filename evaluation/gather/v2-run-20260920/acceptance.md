# CHG-001 V2 验收记录

日期：2026-09-20。Gather 业务提交 e42fefc9ef8171b716c61931ebceb94e02d1aad6，原型提交 5406b97。没有推送或部署。

## 正式应用入口

通过 EasyMint 产品计划 UI 操作，没有编辑宿主状态文件：

- revision 15：真实 Builder 完成，57 次工具调用、0 次工具错误；开发完成与验收结论分开。
- revision 17：运行测试并收集证据，30 项用例，退出码 0；尚无本版关联，整体未判定。
- revision 18：审阅实际测试断言后，确认下列六项条件的关联。
- revision 20：2026/9/20 21:12:45 重跑完成，30 项用例、退出码 0，六项显示“关联检查通过”；其余未关联条件保持未判定，整体未判定。

| 条件 | 实际关联测试 |
|---|---|
| 首次报名成功生成一条记录 | app/App.test.tsx：成员关键交互 可用身份成功报名并立即更新资格与人数 |
| 同场重复拒绝、人数与记录不变 | app/domain/domain.test.ts：集中纯函数命令 同一用户可报名不同活动，但同一活动只能报名一次；app/App.test.tsx：成员关键交互 已有报名身份仍可报名其他活动，同一活动重复提交被拒绝 |
| 跨活动成功、分别更新名单与人数 | 同上两项；领域断言 U2 的完整活动数组及 A1/A2 人数，组件断言保存的全部用户关系 |
| 重复失败不影响其他既有报名 | app/domain/domain.test.ts：集中纯函数命令 同一用户可报名不同活动，但同一活动只能报名一次 |
| 其他符合条件的身份仍可报名 | 同上领域用例：U2 已报名 A2，仍可报名开放且有余位的 A1 |
| 已报名提示不暗示全局禁报、其他活动可提交 | app/App.test.tsx：成员关键交互 已有报名身份仍可报名其他活动，同一活动重复提交被拒绝 |

测试关联仅说明这些断言的实际结果，不是穷尽输入证明。尚未关联的条件不等同于已发现功能失败。例如旧数据迁移另有独立探针及页面观察，但当前正式关联尚未覆盖完整迁移条件；没有通过人工例外把整体改为通过。

## 独立公开探针

同目录 report.json：7/7 通过，含真实 V1 序列化样本迁移、同场拒绝、跨场成功、容量、关闭、重新加载。2026-09-20 复核报告文件清单的全部摘要与当前 Gather 文件一致。报告原始生成时间、提交与工作树信息保持原样。

## 真实 Safari 页面观察

同源 http://127.0.0.1:4173/，没有清空或注入 localStorage：先在 V1 通过 UI 建立 U1→A1，再加载 V2，原有 A1/U1 和 A2/U2/U3 保留。随后创建 A4“V2 跨活动回归”，U1 可报名 A4，重复 A1、关闭 A3、满员 A1 的提交分别被拒绝。U2 对 A1 报名成功，U3 再报名 A1 因容量被拒绝。

本次重新打开同源页：管理员显示 A1=2（U1/U2）、A2=2（U2/U3）、A3=0 关闭、A4=1（U1），概览 5 条报名。切换成员并选择 U1，显示“已报名 2 场”及 A1/A4 名称，两场按钮均为“已报名”；A2 为“活动已满员”，A3 为“报名已关闭”。

边界：这是保留旧数据的真实页面回归，不是 VC-08 指定的 F0/A1/A2 完整序列；没有测窄屏布局、跨设备或并发。没有将这份外部观察冒充 Runner 自动证据。

## 后续验收缺口

补齐其余 P0 条件的断言与语义关联，特别是页面刷新与迁移全状态、关闭既有报名不丢失、非法容量完整枚举和满员页面反馈。执行 VC-08 固定 F0 页面序列；保留现有浏览器报名，可使用隔离测试环境。完成后重跑正式 Runner，不能仅凭本轮 30 项全绿宣称整体需求通过。T6 复盘和 T7 独立对照评测另行推进。

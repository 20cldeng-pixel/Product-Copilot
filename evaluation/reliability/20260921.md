# 2026-09-21 真实应用可靠性验证

方法：通过 Electron 原生界面操作独立项目 `Product Workflow Reliability`。本记录是人工操作观察，不是自动化截图或黑盒 A/B 报告。原 Gather 项目没有改动。

| 场景 | 操作与实际观察 | 结论与边界 |
|---|---|---|
| 启动与取消 | revision 5 确认最小计数器；6 开始 Builder，开发/验收按钮禁用；7 停止，显示等待执行器退出；8 显示已停止、产物保留，开发入口恢复。实际配置 gpt-5.6-sol，0 次工具调用 | 初始化阶段取消真实验证通过；尚不覆盖 shell 写入中强制终止 |
| 验收环境缺失 | revision 10：缺少 node_modules，显示测试执行失败、未判定、0 用例、未知退出码，P0 通过 0/3 | 环境错误未被包装成通过 |
| 原型变更 | 修改原型标题后刷新，旧验收显示失效；尝试启动被主进程拒绝“请先确认当前范围和原型” | 执行保护有效，但旧页面仍显示授权且按钮可点击，发现展示缺陷 |
| 展示缺陷修复 | 新增读取时 developmentCurrent，开发/验收按钮和标题采用当前校验；复盘有效确认数排除失效开发批准。重启后 revision 10 显示开发批准已失效，两个按钮禁用 | 真实界面回归通过；不改历史批准记录 |
| 重新登记与确认 | revision 11 登记修订原型，进入原型待确认；12 确认后开发/验收入口恢复，旧验收仍失效 | 恢复路径通过；命令完成后重新读取派生状态，避免短暂展示旧结果 |
| 写入后取消（自动化持久化探针） | Builder 先写入 `written-before-stop.ts`，随后收到停止信号；服务等待执行器退出后记录 `cancelled/not_run`、新的产物摘要，文件内容保留 | 覆盖已发生文件写入后的合作式取消；不是操作系统强杀 shell 子进程 |
| 进程丢失恢复（自动化持久化探针） | 持久化一个 `running` 记录和部分产物，清空运行时归属后用新服务实例读取；旧运行转为 `interrupted/not_run`，部分文件保留，用户显式重试后新增 `completed` 运行 | 覆盖重启读取与显式重试语义；用新服务实例模拟进程丢失，没有对 Electron 进程执行 SIGKILL |
| Electron 主进程强制退出（真实界面） | 独立可靠性项目在 revision 13 显示“正在开发”后，对 Electron 主进程发送 `SIGKILL`；重启后 revision 14 自动显示“执行中断，产物已保留”，运行历史保留该中断记录，开发与验收入口恢复 | 真实进程死亡恢复通过；发生在 0 次工具调用，未覆盖文件写入或 shell 子进程执行中的强杀 |
| 供应商及预算终态（自动化 SDK 探针） | 供应商请求拒绝、错误终态、缺终局消息均为 `failed`；10 分钟时间上限和 80 次工具调用上限触发 abort 并归为 `cancelled` | 状态与成功分离已覆盖；不是对每一家真实供应商做故障注入 |
| Mint 汇总解读（真实模型） | 先在空业务事件状态调用，模型明确零分母不估算；再导入 10 条 `local_trial` 事件，页面确定性统计为 8→1→1、提交率 12.5%、提交后成功率 100%，模型准确复述并指出模拟身份、未筛选窗口、27 项未判定和零拒绝不能证明异常流程有效 | 空数据与带来源事件各完成一次真实界面/模型调用；未取得 Token、实际费用或多人评审，不能据此声称解释质量提升 |

自动化验证：Builder/Runner/变更服务定向 28 项通过。修复第一版全量 558 项、lint/typecheck、renderer/main 构建通过。最初运行遇到 npm 不在 PATH、旧 Node 原生绑定加载失败，以及全量测试子进程找不到 node；采用 Node 24 运行并为子进程补全 Node/npm PATH 后通过，没有删除锁文件或重装依赖。

仍待真实界面/进程级验证：开发写入期间从页面取消、文件写入或 shell 子进程执行中的强制退出、真实供应商故障注入。Electron 主进程在 0 次工具调用时的 `SIGKILL` 恢复已完成，自动化探针覆盖了写入后的持久化语义；两者合起来仍不能冒充写命令执行中的进程级实验。Mint 已分别完成空数据和带来源事件的单次解读；仍未采集实际 Token、费用、人工耗时或多人解释质量评分。

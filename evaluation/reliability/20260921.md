# 2026-09-21 真实应用可靠性验证

方法：通过 Electron 原生界面操作独立项目 `Product Workflow Reliability`。本记录是人工操作观察，不是自动化截图或黑盒 A/B 报告。原 Gather 项目没有改动。

| 场景 | 操作与实际观察 | 结论与边界 |
|---|---|---|
| 启动与取消 | revision 5 确认最小计数器；6 开始 Builder，开发/验收按钮禁用；7 停止，显示等待执行器退出；8 显示已停止、产物保留，开发入口恢复。实际配置 gpt-5.6-sol，0 次工具调用 | 初始化阶段取消真实验证通过；尚不覆盖 shell 写入中强制终止 |
| 验收环境缺失 | revision 10：缺少 node_modules，显示测试执行失败、未判定、0 用例、未知退出码，P0 通过 0/3 | 环境错误未被包装成通过 |
| 原型变更 | 修改原型标题后刷新，旧验收显示失效；尝试启动被主进程拒绝“请先确认当前范围和原型” | 执行保护有效，但旧页面仍显示授权且按钮可点击，发现展示缺陷 |
| 展示缺陷修复 | 新增读取时 developmentCurrent，开发/验收按钮和标题采用当前校验；复盘有效确认数排除失效开发批准。重启后 revision 10 显示开发批准已失效，两个按钮禁用 | 真实界面回归通过；不改历史批准记录 |
| 重新登记与确认 | revision 11 登记修订原型，进入原型待确认；12 确认后开发/验收入口恢复，旧验收仍失效 | 恢复路径通过；命令完成后重新读取派生状态，避免短暂展示旧结果 |

自动化验证：Builder/Runner/变更服务定向 28 项通过。修复第一版全量 558 项、lint/typecheck、renderer/main 构建通过。最初运行遇到 npm 不在 PATH、旧 Node 原生绑定加载失败，以及全量测试子进程找不到 node；采用 Node 24 运行并为子进程补全 Node/npm PATH 后通过，没有删除锁文件或重装依赖。

仍待真实验证：开发执行中保存/确认变更、写命令期间取消、强制进程退出后的 interrupted 恢复、供应商失败和预算耗尽。普通应用重启已执行，不能冒充运行中崩溃恢复。

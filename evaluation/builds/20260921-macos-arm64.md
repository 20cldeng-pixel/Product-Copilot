# macOS arm64 开发构建记录

日期：2026-09-21。用途：验证当前源码能够生成可挂载的桌面安装镜像；不是正式发布或分发证明。

| 字段 | 结果 |
|---|---|
| 构建命令 | `CSC_IDENTITY_AUTO_DISCOVERY=false npm run build:mac` |
| 本地产物 | `dist-electron/Product Copilot-macOS-arm64.dmg` |
| 源码提交 | `18bfba81413ede2be63b17324d7c9fe524ad0297` |
| 字节数 | `207263960` |
| SHA-256 | `e61adc74d159cb5ee29230295b58d1ee88d4288cf9c15639a00c489399e303ea` |
| 镜像校验 | `hdiutil verify` 通过，checksum valid |
| 架构 | macOS arm64 |
| 签名/公证 | 未签名、未公证；构建日志明确跳过代码签名 |
| 发布状态 | GitHub prerelease：[`product-copilot-v0.1.0-dev`](https://github.com/20cldeng-pixel/Product-Copilot/releases/tag/product-copilot-v0.1.0-dev) |

本轮在挂载镜像后从 DMG 内直接启动 `Product Copilot.app`，实际窗口标题为 `B — Product Copilot`，并读取到原有 B 项目。`CFBundleDisplayName`、`CFBundleName` 和可执行文件名均为 `Product Copilot`；`CFBundleIdentifier` 保持 `com.easymint.app`。打包后的 Pi 配置仍为 `.easymint`。

Electron 用户数据在应用初始化前固定到 `~/.easymint/electron`，因此应用改名后仍读取原设置、项目和窗口状态。自动更新替换脚本同时接受 `Product Copilot.app` 与旧 `EasyMint.app`，旧安装路径升级时仍能就地替换。正式发行前仍需决定是否迁移 npm 包名、bundle identifier、图标和数据目录，并完成 Apple Developer 签名与 notarization。

GitHub 资源名为 `Product.Copilot-macOS-arm64.dmg`，状态为 `uploaded`，远端大小 `207263960` 字节，GitHub 返回的 digest 与本地 SHA-256 一致。旧名称资源已从该 Release 删除。Release 标记为 prerelease，并在安装说明中明确 Apple Silicon、未签名/未公证和非生产发行边界。

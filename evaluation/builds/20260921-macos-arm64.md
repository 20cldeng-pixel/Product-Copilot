# macOS arm64 开发构建记录

日期：2026-09-21。用途：验证当前源码能够生成可挂载的桌面安装镜像；不是正式发布或分发证明。

| 字段 | 结果 |
|---|---|
| 构建命令 | `CSC_IDENTITY_AUTO_DISCOVERY=false npm run build:mac` |
| 本地产物 | `dist-electron/EasyMint-macOS-arm64.dmg` |
| 源码提交 | `60c2261eb4c2b198ac0bd068ef641e775df7204a` |
| 字节数 | `207277268` |
| SHA-256 | `d5eebfc3210cdad93a833e4175b15a05e34231c746ccfc56948c54ed52b81f88` |
| 镜像校验 | `hdiutil verify` 通过，checksum valid |
| 架构 | macOS arm64 |
| 签名/公证 | 未签名、未公证；构建日志明确跳过代码签名 |
| 发布状态 | GitHub prerelease：[`product-copilot-v0.1.0-dev`](https://github.com/20cldeng-pixel/Product-Copilot/releases/tag/product-copilot-v0.1.0-dev) |

本轮在挂载镜像后从 DMG 内直接启动应用，实际窗口标题为 `B — Product Copilot`。同时运行的旧 `/Applications/EasyMint.app` 曾造成一次旧标题误读；关闭旧进程后复核通过。

当前包的 Finder 应用名、DMG 文件名、`CFBundleIdentifier` 和数据/更新路径仍使用上游兼容标识 `EasyMint` / `com.easymint.app`，应用内用户可见品牌已收口为 Product Copilot。正式对外分发前仍需完成包级迁移、图标、自动更新地址、Apple Developer 签名与 notarization；不能只重命名 DMG 文件。

GitHub 资源状态为 `uploaded`，远端大小 `207277268` 字节，GitHub 返回的 digest 与本地 SHA-256 一致。Release 标记为 prerelease，并在安装说明中明确 Apple Silicon、未签名/未公证和非生产发行边界。
